'use strict';

angular.module('mean.articles', []);
/*
angular.module('mean.articles', ['hc.marked', 'hljs']).config(['marked', 'hljs', function(marked, hljs) {
  marked.setOptions({
  	gfm: true,
  	tables: true,
  	highlight: function (code) {
      return hljs.highlightAuto(code).value;
    }
  });
}]);
*/